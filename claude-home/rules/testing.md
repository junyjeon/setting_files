# Testing

- Projects with test infrastructure: new functionality ships with tests.
- Fix the implementation, not the tests (unless the tests are wrong).
- TDD, coverage targets, and E2E scope are per-project decisions — define them in project rules, not globally.
- /verify — run the app after implementing to confirm the change works.
